HOST=0.0.0.0
PORT=8000
BASE_URL=
DATABASE_URL=sqlite+aiosqlite:///data/freecord.db

# Generate a 32-byte urlsafe base64 key: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=XfWHZaEYIQSW8VqfYVTuYPKmJoyPQl3SB41P2R42JC4=

# Generate a random secret string: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=freecord_jwt_secret_change_me_in_production_12345

# Initial admin login credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

ENVIRONMENT=production
DEBUG=false

# Optional custom subdomain for localtunnel if running locally
LOCALTUNNEL_SUBDOMAIN=
